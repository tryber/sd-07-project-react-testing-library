import React from 'react';
import renderWithRender from '../renderWithRouter';
import App from '../App';

describe('Testando o arquivo Pokemon Details', () => {
  it('Deve renderizar Pokemon Details ', () => {
    const { } = renderWithRender(<App />);
  });
});
